// shipguard-lean: keep this fixture tiny for wrapper detection. ceiling: demo-only helper. upgrade: replace when fixture needs multi-call-site proof.
export function toLabel(value: string) {
  return value.trim();
}
