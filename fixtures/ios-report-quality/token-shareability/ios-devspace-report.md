# Devspace Token Shareability Fixture

This fixture intentionally includes a token-bearing connector URL so `shipguard ios report-quality` can prove that report-quality output blocks unsafe sharing without echoing the token value.
